# Example: terraform-aws-vpc

This example demonstrates usage of the `terraform-aws-vpc` module with all key inputs.

## Usage

```bash
terraform init
terraform plan
terraform apply
```

See [`main.tf`](./main.tf) for the full example configuration.
